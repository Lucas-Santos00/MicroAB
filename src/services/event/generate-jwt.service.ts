const generateJWTService = async (jwt:string, teste_uuid:string) => {

    return { 
        code: 200, 
        result: {
            token: "tokensdadas"
        }
    }

};

export default generateJWTService;